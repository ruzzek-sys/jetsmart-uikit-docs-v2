---
title: Buttons
description: "Dos componentes de acción: el botón con texto y el botón circular de solo ícono. Cada uno tiene su propia matriz de variantes y sus propiedades."
figma:
  - "3570:80903"
  - "5167:79401"
props:
  button:
    - name: Size
      type: Variant
      desc: "Large: botón de 52px de alto. Medium: 44px de alto. Small: 36px de alto, con tipografía e íconos más compactos. Large es el valor por defecto."
    - name: State
      type: Variant
      desc: "Default: fondo navy sólido. Hover: fondo navy más oscuro al pasar el cursor. Focused: agrega un anillo azul alrededor del botón. Disabled: fondo y borde gris claro, sin interacción. Default es el valor por defecto."
    - name: Type
      type: Variant
      desc: "Primary: fondo sólido. Secondary: borde con fondo transparente. Tertiary: solo texto e ícono, sin fondo ni borde. Primary es el valor por defecto."
    - name: Intent
      type: Variant
      desc: "Default: fondo navy. Emphasis: fondo rojo para acciones urgentes. Promotional: fondo naranja para contenido promocional. Default es el valor por defecto."
    - name: Inverse
      type: Variant
      desc: "False: fondo oscuro y texto claro, para usar sobre fondos claros. True: invierte la paleta a fondo blanco y texto navy, para usar sobre fondos oscuros. False es el valor por defecto."
    - name: Label
      type: Text
      desc: Texto que se muestra dentro del botón. «Button» es el valor por defecto.
    - name: Show Icon Left
      type: Boolean
      desc: Muestra u oculta el ícono a la izquierda del texto; al desactivarlo, el botón se angosta. Activado por defecto.
    - name: Show Icon Right
      type: Boolean
      desc: Muestra u oculta el ícono a la derecha del texto; al desactivarlo, el botón se angosta. Activado por defecto.
  icon-button:
    - name: Size
      type: Variant
      desc: Large es el tamaño por defecto (52px); Medium (44px) y Small (36px) reducen proporcionalmente el botón, el ícono y el badge para contextos más compactos.
    - name: State
      type: Variant
      desc: Default muestra el botón en reposo. Hover resalta el fill al pasar el cursor. Focused agrega un anillo exterior para navegación por teclado. Disabled reduce la opacidad indicando que la acción no está disponible. Default es el valor por defecto.
    - name: Style
      type: Variant
      desc: Solid rellena el círculo completo con el color de Intent. Outline deja el fondo transparente y solo dibuja el borde y el ícono en ese color. Ghost elimina fondo y borde, dejando solo el ícono coloreado. Solid es el valor por defecto.
    - name: Intent
      type: Variant
      desc: "Define la paleta semántica: Default usa azul marino, Emphasis usa rojo para acciones críticas, Highlight usa turquesa para resaltar y Neutral usa gris para acciones secundarias. Default es el valor por defecto."
    - name: Inverse
      type: Variant
      desc: False usa la paleta estándar pensada para fondos claros. True invierte los colores para mantener el contraste sobre fondos oscuros o de color. False es el valor por defecto.
    - name: Show Badge
      type: Boolean
      desc: Activa una insignia circular roja en la esquina superior derecha para señalar notificaciones pendientes. Oculto (false) es el valor por defecto.
    - name: Badge Count
      type: Text
      desc: Texto mostrado dentro de la insignia cuando Show Badge está activo, normalmente un número corto de notificaciones. Solo visible si Show Badge = True. «1» es el valor por defecto.
    - name: Number
      type: Text
      desc: Texto mostrado en el centro del botón cuando Show Number está activo; convive con el ícono si Show Icon también está activo. «1» es el valor por defecto.
    - name: Show Icon
      type: Boolean
      desc: Muestra u oculta el ícono central (por ejemplo, la flecha). Visible (true) es el valor por defecto.
    - name: Show Number
      type: Boolean
      desc: Muestra un número en el centro del botón, junto al ícono o en su lugar si Show Icon está desactivado. Oculto (false) es el valor por defecto.
tables:
  button-jerarquia-y-color:
    columns:
      - Aspecto
      - Categoría
      - Descripción
    rows:
      - name: Jerarquía visual
        meta:
          - label: Jerarquía
        desc: "Primary usa relleno sólido y concentra el mayor peso visual: se reserva para la única acción principal de la vista. Secondary usa el mismo tamaño y texto en negrita pero con borde y fondo blanco, para acciones alternativas de igual jerarquía funcional pero menor peso. Tertiary no tiene relleno ni borde y reduce su alto, quedando como una acción de bajo compromiso, similar a un enlace."
      - name: Uso de colores
        meta:
          - label: Color
        desc: "El color se aplica por capas: Primary lo aplica al fondo, Secondary solo al borde y texto, Tertiary solo al texto — pero dentro de cada intención, ícono y label comparten siempre el mismo tono. Inverse invierte texto e ícono a blanco para fondos oscuros."
      - name: Default
        meta:
          - label: Intención
        desc: Es la intención por defecto para la mayoría de las acciones.
      - name: Emphasis
        meta:
          - label: Intención
        desc: Se reserva para acciones críticas o que requieren atención inmediata.
      - name: Promotional
        meta:
          - label: Intención
        desc: Se usa para ofertas o mensajes comerciales.
  button-contenido-y-uso-responsive:
    columns:
      - Aspecto
      - Categoría
      - Descripción
    rows:
      - name: Longitud del texto
        meta:
          - label: Caracteres
        desc: "El botón no tiene ancho fijo ni wrap: crece con el contenido (hug), sin truncamiento automático. Por norma de usabilidad se recomienda un máx. de ~20-25 caracteres (1 a 3 palabras) para que quepa en una sola línea y no domine el layout, sobre todo en Small (14px / 36px de alto)."
      - name: Uso en mobile
        meta:
          - label: Responsive
        desc: En mobile siempre se usa el tipo Secondary en lugar de Primary, para aligerar el peso visual de fondos sólidos en pantallas pequeñas.
---

## Button

Botón con texto e íconos opcionales a cada lado. Es el que dispara las acciones de la interfaz, desde la principal de una pantalla hasta las secundarias.

<FigmaEmbed node="3570:80903" title="Button" />

### Resumen

Lleva una etiqueta de texto y hasta dos íconos, uno a cada lado, que se pueden apagar desde la instancia. El peso visual se elige con `Type` (sólido, con borde o solo texto) y el color con `Intent`; `Inverse` adapta la paleta para usarlo sobre fondos oscuros.

216 variantes = `Size` (3) × `State` (4) × `Type` (3) × `Intent` (3) × `Inverse` (2). Las columnas de la matriz recorren `Type`, `Intent` e `Inverse`; las filas, `Size` y `State`.

### Propiedades

Lo que se puede ajustar en cada instancia del botón.

<PropsTable block="button" />

### Guía de uso

#### Jerarquía y color

Cómo comunican peso visual y significado los tres niveles del botón.

<DocTable id="button-jerarquia-y-color" />

#### Contenido y uso responsive

Recomendaciones de largo de texto y comportamiento en mobile.

<DocTable id="button-contenido-y-uso-responsive" />

## Icon Button

Botón circular de solo ícono, para acciones compactas donde no cabe una etiqueta. Puede mostrar un número en el centro y una insignia de notificaciones.

<FigmaEmbed node="5167:79401" title="Icon Button" />

### Resumen

Un círculo con un ícono central que se puede reemplazar o complementar con un número, más una insignia opcional en la esquina para señalar notificaciones pendientes. `Style` define si va relleno, con borde o sin fondo, e `Intent` elige la paleta semántica; `Inverse` mantiene el contraste sobre fondos oscuros.

288 variantes = `Size` (3) × `State` (4) × `Style` (3) × `Intent` (4) × `Inverse` (2). Las columnas de la matriz recorren `Style`, `Intent` e `Inverse`; las filas, `Size` y `State`.

### Propiedades

Propiedades disponibles para configurar el componente Icon Button.

<PropsTable block="icon-button" />
